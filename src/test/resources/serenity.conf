webdriver {
  wait.for.timeout = 14000
  driver=chrome
  timeouts {
    implicitlywait = 10000
    fluentwait = 10000
  }
}

drivers {
  windows {
    webdriver.chrome.driver = src/test/resources/webdriver/windows/chromedriver.exe
  }
  linux {
    webdriver.chrome.driver = src/test/resources/webdriver/linux/chromedriver
  }
}